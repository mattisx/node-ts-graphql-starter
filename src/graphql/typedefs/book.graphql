# Types
type Book implements Timestamps {
  id: ID!
  name: String!
  author: Author!

  # Interface requires these
  createdAt: Date!
  updatedAt: Date!
}

# Inputs
input CreateBookInput {
  name: String!
  author_id: ID!
}

input UpdateBookInput {
  name: String!
  author_id: ID!
}
