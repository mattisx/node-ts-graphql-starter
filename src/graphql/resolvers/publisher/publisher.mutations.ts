import { Context } from '../../../types/serverTypes'

export const publisherMutations = {
  // createPublisher: async (parent: never, args: never, context: Context) => {
  //   const authorRes = await context.services.authorService.getAuthor()
  //   console.log(authorRes)
  //   return authorRes
  // },
  // updatePublisher: async (_, args) => {}
}
