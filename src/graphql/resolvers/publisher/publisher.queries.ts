import { Context } from '../../../types/serverTypes'

export const publisherQueries = {
  // publishers: async (parent: never, args: never, context: Context) => {
  //   const authorRes = await context.services.authorService.getAuthor()
  //   console.log(authorRes)
  //   return authorRes
  // },
  // publisher: async (_, args) => {},
}
